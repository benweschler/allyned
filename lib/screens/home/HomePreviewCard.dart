import 'package:flutter/material.dart';

class HomePreviewCard extends StatelessWidget {
  const HomePreviewCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
